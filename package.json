{
  "name": "control-flotilla-mvp",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Sistema local de flotilla con operaciones, retornos, auditoría y reportes conectados.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=22.12"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "multer": "^2.0.2",
    "pdfkit": "^0.17.2",
    "swagger-ui-express": "^5.0.1"
  }
}
